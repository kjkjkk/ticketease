<div class="card shadow-sm">
    <div class="card-header bg-logo-dark text-light">
        <h{{ $size }} class="fw-bold mt-2">{{ $title }}</h{{ $size }}>
    </div>
    <div class="card-body p-3">
        {{ $slot }}
    </div>
</div>
